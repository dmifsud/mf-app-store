{
  "name": "@mf-app/shared-store",
  "version": "1.0.0",
  "description": "Shared store to be used in module federation",
  "main": "./lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build && tsc",
    "build:types": "tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "preview": "vite preview",
    "serve": "vite build --mode=local-prod && tsc && vite preview --host --mode=local-prod"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@originjs/vite-plugin-federation": "^1.3.5",
    "solid-js": "^1.8.17",
    "vite": "^5.2.11"
  },
  "devDependencies": {
    "fast-glob": "^3.3.2",
    "tslint": "^6.1.3",
    "typescript": "^5.4.5",
    "vite-plugin-solid": "^2.10.2",
    "vite-tsconfig-paths": "^4.3.2"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "typings": "./lib/index.d.ts"
}
